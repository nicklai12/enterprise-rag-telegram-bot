# Render bot.py 專用（精簡版；完整 pipeline 依賴見 requirements-pipeline.txt）
# 查詢端向量化用 fastembed（ONNX Runtime）取代 sentence-transformers/torch：
# 向量一致（cos ≥ 0.999999），峰值記憶體 ~911MB → ~169MB，符合 Render free tier 512MB（issue #47）。
fastembed>=0.2.7,<1.0
python-telegram-bot>=21.0,<22.0
chromadb>=1.0,<2.0
groq>=0.11,<1.0
PyYAML>=6.0,<7.0
